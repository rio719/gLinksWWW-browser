name: GlinksWWW Windows Build

on:
  push:
    branches: [ "main" ] # 메인 브랜치에 푸시될 때 실행
  workflow_dispatch: # 직접 수동으로 'Run' 버튼 눌러서 실행 가능하게 함

jobs:
  build:
    runs-on: windows-latest # 윈도우 서버를 빌려옵니다

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18' # 리오님 환경에 맞는 노드 버전

      - name: Install dependencies
        working-directory: ./glinks-browser-1.2.5 # 👈 리오님이 만든 그 폴더로 이동!
        run: npm install

      - name: Build Electron App
        working-directory: ./glinks-browser-1.2.5
        run: npm run build # package.json에 명시된 빌드 스크립트 실행
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload .exe file
        uses: actions/upload-artifact@v4
        with:
          name: GlinksWWW-Win-Exe
          path: glinks-browser-1.2.5/dist/*.exe # 빌드 결과물이 나오는 경로 (dist 폴더 확인!)
